{
  "name": "birthday-mario",
  "version": "1.0.0",
  "description": "Game ulang tahun: 2 sub-game (pilih foto + Mario dengan quiz)",
  "scripts": {
    "build": "esbuild lib/game.js --bundle --outfile=assets/javascripts/bundle.js",
    "watch": "esbuild lib/game.js --bundle --outfile=assets/javascripts/bundle.js --watch",
    "start": "esbuild lib/game.js --bundle --outfile=assets/javascripts/bundle.js --servedir=. --serve=127.0.0.1:3005"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
